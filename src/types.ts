export type OneClickViewConfig = {
  selector: string;
  zoomLevel?: number;
  transitionDuration?: number;
  modalBackground?: string;
  modalZIndex?: number;
};

export type ModalConfig = {
  backgroundColor: string;
  zIndex: number;
};
